language: java

jdk:
  - openjdk8

services:
  - docker

script:
  - docker build -t jecklgamis/dropwizard-kotlin-example .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push jecklgamis/dropwizard-kotlin-example:latest