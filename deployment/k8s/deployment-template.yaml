apiVersion: v1
kind: Service
metadata:
  name: "{{ app_name }}"
  labels:
    app: "{{ app_name }}"
spec:
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      name: http
    - port: 443
      protocol: TCP
      targetPort: 8443
      name: https
  selector:
    app: "{{ app_name }}"

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ app_name }}"
spec:
  selector:
    matchLabels:
      app: "{{ app_name }}"
  replicas: 2
  template:
    metadata:
      labels:
        app: "{{ app_name }}"
    spec:
      containers:
        - name: "{{ app_name }}"
          image: "jecklgamis/{{ app_name }}:{{ version }}"
          ports:
            - containerPort: 8080
            - containerPort: 8443

